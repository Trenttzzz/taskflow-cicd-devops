apiVersion: v1
kind: Service
metadata:
  name: taskflow-api
  namespace: taskflow-prod
  labels:
    app: taskflow-api
spec:
  type: NodePort
  selector:
    app: taskflow-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30080
